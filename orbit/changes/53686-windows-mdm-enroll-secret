- Added support on Windows for an enroll secret delivered by Fleet MDM through the registry, so fleetd no longer needs the secret on the installer command line and an administrator can recover a host by resending the profile that carries it.
