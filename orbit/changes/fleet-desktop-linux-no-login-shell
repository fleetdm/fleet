* Fixed Fleet Desktop failing to start on openSUSE Leap 16 (and other distributions whose login-shell profile scripts interfere with the inline command), by dropping the `-i` (login shell) flag from the sudo invocation used to launch Fleet Desktop and key-escrow dialogs as the logged-in user.
