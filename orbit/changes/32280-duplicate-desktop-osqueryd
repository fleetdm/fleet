* Since new macOS/Linux packages built with `fleetctl 4.75.0` or higher do not have embedded osqueryd.tar.gz and desktop.tar.gz, orbit can now use osqueryd.tar.gz.sha512 and desktop.tar.gz.sha512 hash caches to check if an update is needed.
