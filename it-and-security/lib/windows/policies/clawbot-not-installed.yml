- name: Windows - Clawbot not installed
  query: SELECT 1 WHERE NOT EXISTS (SELECT 1 FROM programs WHERE name LIKE '%Clawbot%');
  critical: false
  description: "Checks that Clawbot is not installed on Windows hosts."
  resolution: "Uninstall Clawbot from this device."
  platform: windows
  calendar_events_enabled: false
