- name: macOS - Desktop background is a photo of @alex
  query: "SELECT 1 FROM plist WHERE path = '/Users/' || (SELECT user FROM logged_in_users ORDER BY time DESC LIMIT 1) || '/Library/Preferences/com.apple.desktop.plist' AND value LIKE '%alex-mitchell.png%';"
  critical: false
  description: "Checks that the desktop background is set to alex-mitchell.png."
  resolution: "The desktop background will be automatically set to alex-mitchell.png."
  platform: darwin
  calendar_events_enabled: false
  run_script:
    path: ../../macos/scripts/set-desktop-background-alex.sh
