## 0. Spike (de-risk before main implementation)

See `ai/android-commands/spike-plan.md` for the full plan. Output: a measurements doc that may revise design.md decisions
on operation-name lengths, Pub/Sub COMMAND payload shape, and command-expiry behavior.

- [ ] 0.1 Implement the minimum-viable IssueCommand path on a feature branch — just enough to send a LOCK to one BYO + one
      COBO test device and capture the Pub/Sub COMMAND notification payload. Throwaway code; outputs are measurements,
      not production-ready code.
- [ ] 0.2 Record measurements in `ai/android-commands/spike-results.md`: operation name length, Pub/Sub payload shape,
      time from issue to ack on BYO vs COBO, RESET_PASSWORD with empty newPassword behavior on both ownership modes,
      command-expiry behavior at 10-minute default duration.
- [ ] 0.3 Revise `design.md` if the spike reveals anything different from assumptions (most likely candidates: janitor-cron
      decision, operation-name column width, RESET_PASSWORD semantics).

## 1. Database migration

- [ ] 1.1 Run `make migration name=AddMDMAndroidCommandsTable`, edit the generated migration to create the `mdm_android_commands`
      table per design.md storage shape. Indexes on `host_uuid` and `operation_name`. Use `DATETIME(6) NOT NULL DEFAULT NOW(6)`
      / `DATETIME(6) NOT NULL DEFAULT NOW(6) ON UPDATE NOW(6)` for `created_at` / `updated_at`. Down function is a no-op.
- [ ] 1.2 Write the migration test under `server/datastore/mysql/migrations/tables/`: `applyUpToPrev(t)` -> set up data ->
      `applyNext(t, db)` -> verify columns exist and indexes are present.

## 2. Backend types

- [ ] 2.1 Add `MDMAndroidCommand` struct to `server/mdm/android/android.go` mirroring the column shape, with `db:` tags
      matching the table.
- [ ] 2.2 Add `MDMAndroidCommandStatus` constants (`pending`, `acknowledged`, `error`) and `MDMAndroidCommandType`
      constants (`LOCK`, `RESET_PASSWORD`, `WIPE`) in the same file.

## 3. AMAPI client capability

- [ ] 3.1 Add `EnterprisesDevicesIssueCommand(ctx context.Context, deviceName string, command *androidmanagement.Command)
      (*androidmanagement.Operation, error)` to the `Client` interface in
      `server/mdm/android/service/androidmgmt/client.go`.
- [ ] 3.2 Implement in `google_client.go` — single call to `g.mgmt.Enterprises.Devices.IssueCommand(deviceName, command).Context(ctx).Do()`.
- [ ] 3.3 Implement in `proxy_client.go` — proxies through the existing licensed-proxy flow.
- [ ] 3.4 Regenerate the mock in `server/mdm/android/mock/client.go` via `go generate ./...` (the file is auto-generated by
      `mockimpl`).

## 4. Datastore methods

- [ ] 4.1 Add `NewMDMAndroidCommand(ctx, cmd *android.MDMAndroidCommand) error` to `fleet.Datastore` interface, implement in
      `server/datastore/mysql/android.go`. Inserts into `mdm_android_commands`.
- [ ] 4.2 Add `GetMDMAndroidCommandByUUID(ctx, commandUUID string) (*android.MDMAndroidCommand, error)`.
- [ ] 4.3 Add `GetMDMAndroidCommandByOperationName(ctx, operationName string) (*android.MDMAndroidCommand, error)` for the
      Pub/Sub lookup path.
- [ ] 4.4 Add `UpdateMDMAndroidCommandStatus(ctx, commandUUID string, status string, errorCode *string, errorMessage *string) error`.
- [ ] 4.5 Extend `GetHostLockWipeStatus` in `server/datastore/mysql/scripts.go:1456` with a `case "android":` arm that reads
      the referenced `mdm_android_commands` row and populates the same status object the Apple branch does. Add an analogous
      branch in `GetHostsLockWipeStatusBatch` (`server/datastore/mysql/scripts.go:1612`).
- [ ] 4.6 Extend the existing `host_mdm_actions` write path used by Apple/Windows to accept the Android UUID format and
      `fleet_platform = 'android'`. May not need any code change — verify the existing `bulkSetPendingMDM*` functions are
      generic enough.

## 5. android.Service new methods

- [ ] 5.1 Add three methods to the `android.Service` interface in `server/mdm/android/service.go`:
      `LockAndroidHost`, `WipeAndroidHost`, `ClearAndroidPasscode`. Each signature
      `(ctx context.Context, hostID uint) (commandUUID string, err error)`.
- [ ] 5.2 Implement `LockAndroidHost` in `server/mdm/android/service/service.go`. Flow: resolve host, resolve enterprise +
      device name, generate `command_uuid` (uuid.NewString), call
      `androidAPIClient.EnterprisesDevicesIssueCommand(ctx, deviceName, &androidmanagement.Command{Type: "LOCK"})`. Store
      operation.Name + command_uuid in `mdm_android_commands` (status=pending). Update
      `host_mdm_actions.lock_ref = command_uuid` and `fleet_platform = 'android'`. Return command_uuid.
- [ ] 5.3 Implement `WipeAndroidHost` analogously with `Type: "WIPE"`, writing `host_mdm_actions.wipe_ref`. Server-side
      precondition (enforced in EE service layer, not here): only callable for COBO Android.
- [ ] 5.4 Implement `ClearAndroidPasscode` analogously with `&androidmanagement.Command{Type: "RESET_PASSWORD", NewPassword: ""}`.
      Does NOT write `host_mdm_actions` (Clear passcode is fire-and-flash, no pending badge).
- [ ] 5.5 Modify `UnenrollAndroidHost` (`server/mdm/android/service/service.go:823`): branch on `companyOwned`:
      - **BYO**: generate a `command_uuid`, call `IssueCommand(WIPE)`, insert a row in `mdm_android_commands`
        (`command_type='WIPE'`, `status='pending'`). Do NOT write `host_mdm_actions.wipe_ref` (no Android badge to drive,
        no user-facing wipe state). Do NOT emit a new activity — the existing `MDMUnenroll` caller continues to fire
        `ActivityTypeMDMUnenrolled` unchanged (product decision 2026-05-20: BYO Unenroll emits the previous unenroll
        activity).
      - **COBO**: unchanged — `EnterprisesDevicesDelete`.

## 6. Pub/Sub COMMAND handler

- [ ] 6.1 In `server/mdm/android/service/pubsub.go:65`, add a case for `android.PubSubCommand` that calls a new
      `handlePubSubCommand(ctx, token, rawData)`.
- [ ] 6.2 Implement `handlePubSubCommand`:
      - Authenticate via existing `authenticatePubSub`.
      - Decode `rawData` as `androidmanagement.Command` (the AMAPI Command resource carried in the notification).
      - Look up `mdm_android_commands` row by `operation_name` (the notification carries the operation name; the message
        attributes also include `name` and the Command's URL-path equivalent).
      - On success (no `errorCode`): set `status = 'acknowledged'`. For LOCK / explicit WIPE (rows whose corresponding
        `host_mdm_actions.lock_ref` or `wipe_ref` matches the row's command_uuid), clear that reference so the cross-platform
        state advances (matches Apple's "command acknowledged → state advances" semantics). For RESET_PASSWORD and BYO
        Unenroll's WIPE, no `host_mdm_actions` change is needed (no ref was set at issue time).
      - On failure (`errorCode` set): set `status = 'error'`, persist `error_code` and `error_message`, leave any
        `host_mdm_actions` reference in place (admin sees the failed command lingering in pending state and can re-issue),
        log via `logger.WarnContext` (a failed Lock should not page anyone but should appear in logs).
- [ ] 6.3 Add unit tests in `server/mdm/android/service/pubsub_test.go` for the new case: success ack of LOCK / WIPE /
      RESET_PASSWORD, failure ack with INVALID_VALUE / API_LEVEL errors, and the "ignore unknown command_uuid" path
      (notification for a command Fleet doesn't track — should be a no-op, not an error).

## 7. EE service dispatch

- [ ] 7.1 In `ee/server/service/hosts.go:355` (`enqueueLockHostRequest`), add `case "android":` calling
      `svc.androidSvc.LockAndroidHost(ctx, host.ID)`. Activity: existing `ActivityTypeLockedHost` with `ViewPIN: false`.
- [ ] 7.2 In `ee/server/service/hosts.go:492` (`enqueueWipeHostRequest`), add `case "android":` calling
      `svc.androidSvc.WipeAndroidHost(ctx, host.ID)`. Activity: existing `ActivityTypeWipedHost`.
- [ ] 7.3 In `ee/server/service/hosts.go:46` (`LockHost`), add `case "android":` validation:
      - MDM enabled and configured (use `appCfg.MDM.AndroidEnabledAndConfigured`).
      - Host must be connected to Fleet MDM (use existing helper).
      - No BYO-vs-COBO restriction for Lock (both can be locked).
- [ ] 7.4 In `ee/server/service/hosts.go:242` (`WipeHost`), add `case "android":` validation:
      - MDM enabled and configured.
      - Host must be COBO (reject BYO with a clear error message: "Wipe is not supported for personally-owned Android hosts.
        Use Unenroll instead.").
- [ ] 7.5 Restructure `ee/server/service/mdm.go:1810` `ClearPasscode`: change from `if fleet.IsApplePlatform(host.Platform)`
      to `switch host.FleetPlatform()`. Add `case "darwin", "ios", "ipados":` calling existing `clearPasscodeApple`. Add
      `case "android":` calling `svc.androidSvc.ClearAndroidPasscode`. Emit `ActivityTypeClearedPasscode`. Validation:
      MDM enabled and configured; no BYO-vs-COBO restriction.

## 8. Frontend: actions dropdown + badge gating

- [ ] 8.1 In `frontend/pages/hosts/details/HostDetailsPage/HostActionsDropdown/helpers.tsx`, update `canLockHost`,
      `canWipeHost`, `canUnlock`, `canClearPasscode`. For each: keep the existing `!isAndroid(hostPlatform)` exclusion only
      where it currently makes sense (Unlock — Android has no unlock); replace others with Android-aware logic per the
      design.md "Dropdown visibility per ownership mode" table.
- [ ] 8.2 Add `isAndroidMdmEnabledAndConfigured` to the config check where dropdown items use `isMacMdmEnabledAndConfigured`
      / `isWindowsMdmEnabledAndConfigured`.
- [ ] 8.3 Update the dropdown tests in `HostActionsDropdown.tests.tsx` to cover the new Android cases: BYO with MDM on,
      COBO with MDM on, Android with MDM off. Assert visible options match the Figma dropdown screenshots.
- [ ] 8.4 Gate the "Wipe pending" / "Wiped" badge rendering on the host details page header to exclude Android. Find the
      badge-rendering code (likely in `frontend/pages/hosts/details/HostDetailsPage/HostDetailsPage.tsx` or a nearby header
      component that reads `host.mdm.device_status`) and add an `!isAndroid(hostPlatform)` guard. Add a test asserting the
      badge does NOT render for an Android host even when the underlying device status is `wipe_pending` or `wiped`.
      Reference: product decision 2026-05-20 — no host-header badges for Android.

## 9. Frontend: modals

- [ ] 9.1 Add Android-platform copy variants to the existing Lock modal. Copy: `Locking will enforce the host lock screen
      and require the user to enter their password/PIN to regain access.` (same as iOS/macOS — no Android-specific change).
      Confirm checkbox: `I wish to lock {host_name}`.
- [ ] 9.2 Add an Android-platform Wipe modal variant: `All content will be erased on this host.` Confirm checkbox:
      `I wish to wipe {host_name}`. Button label: `Wipe`. Show only for COBO Android (BYO uses Unenroll).
- [ ] 9.3 Add Android-platform Clear-passcode modal variants — two flavors:
      - **BYO**: body `This only clears the work profile passcode.`
      - **COBO**: body `This will clear the host passcode. The user can unlock the device without entering a passcode.`
      - Both: confirm checkbox `I wish to clear the passcode for {host_name}`, button `Clear passcode`.
- [ ] 9.4 `UnenrollMdmModal.tsx` Android branch (lines 111-120): no copy change required. Product confirmed 2026-05-20
      that BYO Unenroll continues to emit `ActivityTypeMDMUnenrolled` and no badge is shown — so the existing modal copy
      ("Company data and OS settings (work profile) will be deleted.") remains consistent with the user-facing
      experience. Skip this task and remove from QA scope.
- [ ] 9.5 Flash messages — wire each modal to render via the existing `renderFlash` per Figma:
      - Lock success: `Successfully sent request to lock this host.`
      - Lock error: `Couldn't send request to lock this host. Please try again.`
      - Wipe success/error: analogous.
      - Clear passcode success/error: analogous.

## 10. fleetctl

- [ ] 10.1 In `cmd/fleetctl/fleetctl/mdm.go`, update the help text for `mdmLockCommand` (line 167) and `mdmWipeCommand`
      (line 254) to mention Android support. Confirm the existing flow works — these subcommands just hit the REST endpoint,
      which now dispatches by platform on the server.
- [ ] 10.2 Add a new `mdmClearPasscodeCommand` subcommand following the same shape as `mdmLockCommand`. Wraps
      `POST /hosts/:id/clear_passcode`. Help text references both iOS/iPadOS and Android support.
- [ ] 10.3 Register the new subcommand in the `mdmCommand` (around line 29 — beside `mdmUnlockCommand`).
- [ ] 10.4 Update `MDMLockHost`, `MDMWipeHost` in `server/service/client_mdm.go:406,422` if any platform-aware behavior needs
      adjusting (likely no change — these just POST to the endpoint).
- [ ] 10.5 Add a fleetctl integration test in `cmd/fleetctl/fleetctl/mdm_test.go` exercising the clear-passcode happy path
      for Android.

## 11. Integration tests

- [ ] 11.1 In `server/service/integration_android_*_test.go`, add a new test file `integration_android_commands_test.go`.
- [ ] 11.2 Test cases per command × ownership matrix:
      - LOCK on BYO Android: enqueue → assert AMAPI client called → simulate Pub/Sub COMMAND ack → assert
        `mdm_android_commands.status='acknowledged'` and `host_mdm_actions.lock_ref` cleared.
      - LOCK on COBO Android: same flow.
      - WIPE on COBO: enqueue → ack → `mdm_android_commands.status='acknowledged'`, `host_mdm_actions.wipe_ref` advanced.
      - WIPE on BYO: assert rejected with 400 "Wipe is not supported for personally-owned Android hosts."
      - RESET_PASSWORD on BYO: enqueue → ack → `ActivityTypeClearedPasscode` logged → no `host_mdm_actions` change.
      - RESET_PASSWORD on COBO: same flow.
      - BYO Unenroll: enqueue → WIPE command sent (not device.delete) → `mdm_android_commands` row created with
        `command_type='WIPE'` → Pub/Sub COMMAND ack → `status='acknowledged'`. Assert `ActivityTypeMDMUnenrolled` was
        emitted (NOT `ActivityTypeWipedHost`) and `host_mdm_actions.wipe_ref` was NOT set.
      - COBO Unenroll: device.delete sent, unchanged behavior, `ActivityTypeMDMUnenrolled` emitted.
      - **Frontend: Android badge gating.** Set up an Android host with `wipe_pending` or `wiped` device status via direct
        DB write, render the host details page, assert no "Wipe pending" / "Wiped" badge appears next to the host name.
        Then set the same status on an iOS host and assert the badge DOES appear (regression check that the gating didn't
        break iOS).
- [ ] 11.3 Failure-path tests: Pub/Sub COMMAND notification with `errorCode = "UNSUPPORTED"` → command marked error,
      host stays unlocked, no activity-success entry.
- [ ] 11.4 Pending-state guard tests: enqueue LOCK while another LOCK is pending → reject with 422 "Host has pending lock
      request."
- [ ] 11.5 Premium gate tests: free-tier instance, attempt Lock/Wipe/Clear-passcode → 402 ErrMissingLicense (same as
      existing Apple).

## 12. Documentation

- [ ] 12.1 Update https://fleetdm.com/guides/lock-wipe-hosts (file in `articles/` or `docs/` — check current path) with an
      Android section.
- [ ] 12.2 Confirm `docs/Contributing/reference/audit-logs.md` examples for `locked_host`, `wiped_host`, `cleared_passcode`
      activities include Android-platform examples.
- [ ] 12.3 Add a `changes/` file per `docs/Contributing/guides/committing-changes.md#changes-files`.

## 13. QA + risk mitigation

- [ ] 13.1 Hold a pre-merge real-device QA pass per the spike plan: validate Lock/Wipe/Clear-passcode/Unenroll on one BYO
      Pixel + one COBO Pixel. Capture timing data for the "Wipe pending" badge appearance and dismissal.
- [ ] 13.2 Confirm the Pub/Sub COMMAND notification arrives reliably for all four commands in real-device QA. If any
      command fails to emit a notification (notably command expiry at 10 min default), file a follow-up for the janitor
      cron.
- [ ] 13.3 Pre-QA load test: add 100 Android hosts with concurrent Lock commands in the load-test harness. Assert host
      details page latency stays within current SLO (the new `mdm_android_commands` join in `GetHostLockWipeStatus` is the
      main risk).
