---
apiVersion: v1
kind: query
spec:
  description: Returns the private keys which are not encrypted for domain accounts
  name: ssh_keys_unencrypted_domain_accounts
  query: SELECT uid, username, description, path, encrypted FROM users cross join user_ssh_keys using (uid) WHERE encrypted=0 and username in (SELECT distinct(username) FROM
    last);;
