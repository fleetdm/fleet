<?xml version="1.0" encoding="utf-8"?>
<restrictions xmlns:android="http://schemas.android.com/apk/res/android">

    <restriction
        android:key="enrollSecret"
        android:title="@string/enroll_secret_title"
        android:restrictionType="string"
        android:description="@string/enroll_secret_description" />

    <restriction
        android:key="fleetBaseUrl"
        android:title="@string/fleet_base_url_title"
        android:restrictionType="string"
        android:description="@string/fleet_base_url_description" />

    <restriction
        android:key="certificateIDs"
        android:title="@string/certificate_ids_title"
        android:restrictionType="string"
        android:description="@string/certificate_ids_description" />

    <restriction
        android:key="enrollmentSpecificID"
        android:title="@string/enrollment_specific_id_title"
        android:restrictionType="string"
        android:description="@string/enrollment_specific_id_description" />

    <restriction
        android:key="test_array"
        android:title="test"
        android:restrictionType="bundle_array" >
        <restriction
            android:key="array_key"
            android:title="array key"
        />
    </restriction>
</restrictions>