name:
description:
resolution:
query: "SELECT 1 WHERE NOT EXISTS (SELECT 1 FROM apps WHERE bundle_identifier = 'com.adobe.acc.AdobeCreativeCloud' AND bundle_short_version != '$FMA_VERSION' AND name != 'this is an override policy we downloaded from the manifest');"
