meta {
  name: Delete label by ID
  type: http
  seq: 8
}

delete {
  url: {{host}}/api/{{api_version}}/fleet/labels/id/:id
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: FAIL
}
