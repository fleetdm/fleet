meta {
  name: Modify label
  type: http
  seq: 2
}

patch {
  url: {{host}}/api/{{api_version}}/fleet/labels/:id
  body: json
}

body:json {
  {
    "name": "macOS label",
    "description": "Now this label only includes macOS machines",
    "platform": "darwin"
  }
}
headers {
  Authorization: Bearer {{token}}
}