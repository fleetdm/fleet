meta {
  name: Get host OS versions
  type: http
  seq: 14
}

get {
  url: {{host}}/api/{{api_version}}/fleet/os_versions
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: 200
}
