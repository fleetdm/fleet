meta {
  name: Get host by identifier
  type: http
  seq: 5
}

get {
  url: {{host}}/api/{{api_version}}/fleet/hosts/identifier/{{host_1_uuid}}
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: 200
}
