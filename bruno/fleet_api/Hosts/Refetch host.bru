meta {
  name: Refetch host
  type: http
  seq: 7
}

post {
  url: {{host}}/api/{{api_version}}/fleet/hosts/{{host_id}}/refetch
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: 200
}
