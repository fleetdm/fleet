meta {
  name: Get host
  type: http
  seq: 4
}

get {
  url: {{host}}/api/{{api_version}}/fleet/hosts/{{host_id}}
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: 200
}

script:post-response {
  bru.setVar("host_1_uuid", res.body.host.uuid)
}
