meta {
  name: Get host-s Google Chrome profiles
  type: http
  seq: 11
}

get {
  url: {{host}}/api/{{api_version}}/fleet/hosts/{{host_id}}/device_mapping
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: 200
}
