name: osquery_info
examples: |-
  Retrieve osquery agent information reported by an Android device.
  
  ```
  SELECT version, config_hash FROM osquery_info;
  ```
notes: |-
 
  - This table reports metadata about the osquery agent running on the Android device.
  - Values are provided by the Fleet Android agent and reflect the currently running osquery build.
  - This information is useful for debugging agent deployments and verifying expected osquery versions.

