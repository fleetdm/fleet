name: login_password
platforms:
  - macOS
description: Options of login and password (e.g password hints enabled).
columns:
  - name: password_hint_enabled
    type: integer
    required: false
    description: whether password hint is enabled. 1 is enabled. 0 is not enabled.
notes: >-
  - This table is not a core osquery table. It is included as part of Fleetdm, the osquery manager from Fleet.
evented: false