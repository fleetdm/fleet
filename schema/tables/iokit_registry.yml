name: iokit_registry
examples: |-
  Identify devices with a Yubikey connected. The name will also contain the
  protocols supported by the key, such as FIDO.

  ```
  SELECT * from iokit_registry WHERE name LIKE 'Yubi%';
  ```
