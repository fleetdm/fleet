{{/* Android devices don't use osquery templates. This file exists only so that
     template.ParseFS succeeds when "android" is specified in --os_templates.
     The android agent communicates with Fleet via PubSub messages, not osquery endpoints. */}}
