* Added support for host vitals labels based on host table columns (`public_ip`, `hardware_model`, and `hardware_vendor`), in addition to the existing IdP vitals. This allows, for example, labeling hosts by the office network they connect from via their public IP address.
* Added a `LIKE` operator for host vitals label criteria (in addition to the default `=`), enabling SQL wildcard pattern matching on vital values.
