- Fixed slow load times and timeouts on the list MDM commands API (`GET /api/v1/fleet/commands` and `GET /api/v1/fleet/mdm/commands`) on Fleet deployments with many Windows hosts. The endpoint now caps `per_page` at 1,000 (requests with a larger value return HTTP 400) and defaults to 10 results when `per_page` is not specified.
