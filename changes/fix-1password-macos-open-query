* Fixed the macOS 1Password Fleet-maintained app always being detected as open. 1Password's browser helper and launcher are login items that live inside `1Password.app` and keep running after the app is quit, so the check now matches only the app's own executable.
