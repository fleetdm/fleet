- Updated the SQL editor's syntax-highlighting colors to match Fleet's brand palette (keywords, columns, strings, numbers, punctuation, comments, and errors), fixed several missing SQL keywords (`like`, `using`, `in`, `distinct`, `between`, `exists`, `is`, `all`) that were rendering as unstyled identifiers, and fixed a couple of visual bugs (a lingering focus drop-shadow and a grey sliver visible around the editor in its default state).
