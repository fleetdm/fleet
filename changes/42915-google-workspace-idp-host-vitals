- Added a Google Workspace integration (Fleet Premium) that syncs IdP users and groups directly from the Google Workspace Admin SDK Directory API using a service account with domain-wide delegation, populating IdP host vitals (`end_user_idp_department`, `end_user_idp_group`) without requiring SCIM or a third-party bridge. Configure it under `integrations.google_workspace` in app config / GitOps.
