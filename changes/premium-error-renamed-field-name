- Fixed the free-tier premium license error naming a renamed field by its old name when the request used the new one. For example, sending `fleet_ids` to `POST /api/v1/fleet/automations/reset` now reports `fleet_ids` instead of `team_ids`.
