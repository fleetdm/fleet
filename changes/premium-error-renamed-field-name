- Fixed the free-tier premium license error naming a renamed field by its old name. It now uses the current name whichever one the request sent. For example, `POST /api/v1/fleet/automations/reset` reports `fleet_ids` instead of `team_ids`.
