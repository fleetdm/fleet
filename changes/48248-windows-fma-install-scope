- Fixed Windows Fleet-maintained apps installing a duplicate copy at a different scope (per-user vs. per-machine), which left a stale copy behind and kept the patch policy reporting the host as not patched. The PowerToys and GIMP install scripts now remove any per-user copy before installing the managed per-machine copy, and all Windows Fleet-maintained apps now declare an explicit install scope.
