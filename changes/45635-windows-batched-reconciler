- Reworked Windows MDM configuration profile reconciliation to compute install/remove deltas in memory from an indexed snapshot instead of a fleet-wide set-difference query, and to drain work across the whole enrolled-host set within a tick. This collapses single-change delivery latency and removes the host-discovery query that dominated reconcile DB load during bulk events like team transfers.
