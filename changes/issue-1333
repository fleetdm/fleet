- Schedule page functionality: Create (modal), delete (modal), view schedule, advanced options
- Includes empty state and error state
- Replaces Packs tab with Schedules tab
- Updates e2e test queryflow.spec.ts to include Schedules
- Updates all RBAC e2e test to test access to Schedules
- Refactors `TableContainer` ActionButton CTA to allow custom text 
- Adds OPTIONS to `utilities/constants` and utilize across packs and Schedules
- Defaults logging type to snapshot for packs
- Builds out routing to endpoint for GlobalScheduledQueries
- Builds out global_scheduled_queries methods
- Tests global_scheduled_queries Redux entities
- Adds conversion helpers and tests helper functions
- Adds global_scheduled_queries to `frontend/interfaces`


Implements #369