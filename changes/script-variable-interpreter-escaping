- Fixed an issue where characters in a Fleet variable's value could change what a script does. Values are now defined at the top of the script and read as literal text.
- Fleet variables in scripts are no longer substituted inside single quotes or a quoted heredoc.
- Fleet variables are no longer supported in Python scripts, or in PowerShell scripts that start with a `param()` block.
