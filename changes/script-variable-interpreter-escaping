- Fixed an issue where a host's resolved Fleet variables (for example IdP attributes) could reach a script's contents without being escaped for the target interpreter. Values are now defined at the top of the script, so characters that are meaningful to the interpreter are treated as literal text. A `$FLEET_VAR_*` reference inside single quotes or a quoted heredoc is no longer substituted, and Fleet variables are no longer supported in Python scripts.
