- Fleet now restores BitLocker protection on Windows hosts that are encrypted but whose protection is off, where disk encryption is enforced. Such a host now shows "Enforcing" while Fleet repairs it, and only "Action required" once fleetd reports it cannot, in which case the host's disk encryption details give the reason.
