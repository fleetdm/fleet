- Added `signature_information` with `executable_path` and `executable_sha256` for each Mach-O executable installed by a Homebrew formula under its keg's `bin` and `sbin`, for use with Santa binary rules. Requires an updated fleetd.
- Added per-executable SHA-256 hashes to the host software page: the **Hash** column falls back to the executable hash when a title has no cdhash, and the version details list each Mach-O executable with its hash and a copy button.
