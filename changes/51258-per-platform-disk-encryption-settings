- Added per-platform disk encryption settings for macOS (enforcement and key escrow), Windows, and Linux (key escrow) to the global and fleet configuration. The flat `mdm.enable_disk_encryption` setting is deprecated: it now reads as true only when every per-platform setting is on, and writing it applies to all platforms.
