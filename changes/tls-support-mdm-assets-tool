* Added TLS support to the MDM assets tool, configurable via command-line flags (`-tls-config`, `-tls-ca`, `-tls-cert`, `-tls-key`, `-tls-server-name`), environment variables (`ASSETS_DB_TLS_*`), or a `.env` file.
