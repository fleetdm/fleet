- Added `auth.mdm_windows_one_time_enroll_secrets`. When enabled, Windows MDM hosts are sent a per-device, single-use enroll secret instead of the shared global enroll secret. The secret is delivered by a Fleet-managed "Fleetd enroll secret" configuration profile, and an administrator resending that profile is how a host whose secret was already used gets another.
