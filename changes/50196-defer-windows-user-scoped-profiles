- Windows user-scoped configuration profiles (`./User/...`) are now held in "Pending" until a user signs in, instead of failing during setup. This applies to hosts enrolled by a user (Windows Autopilot, Entra ID).
