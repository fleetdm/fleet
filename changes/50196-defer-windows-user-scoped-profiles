- Windows user-scoped configuration profiles (`./User/...`) are now held until a user signs in, instead of failing during setup. On hosts enrolled by a user (Windows Autopilot, or joining Microsoft Entra ID during setup), these profiles stay "Pending" through setup and are delivered automatically once the first user signs in, rather than failing before anyone can sign in.
