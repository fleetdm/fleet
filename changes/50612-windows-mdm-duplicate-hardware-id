- Fixed a bug where a Windows host silently lost MDM management when a second host enrolled presenting the same MDM hardware ID (`HWDevID`), which happens when two machines' Windows images share an ancestor that was cloned without running `sysprep /generalize`. Both hosts now keep their own MDM enrollment, and Fleet logs a warning naming both hosts when it sees a duplicate hardware ID.
