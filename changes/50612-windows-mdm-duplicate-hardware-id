- Fixed a bug where a Windows host silently lost MDM management, including its configuration profiles and any recorded lock or wipe state, when a second host enrolled presenting the same MDM hardware ID (`HWDevID`). Both hosts now keep their own MDM enrollment, and Fleet logs a warning naming both hosts.
