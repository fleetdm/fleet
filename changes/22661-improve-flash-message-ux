* Allow clicking in the horizontal dimension of a flash message, outside of the message itself, and
always hide flash messages when changing routes.
* Refactor `renderFlash` and associated logic to allow a `dismissOnPageChange` option, which ensures
  such flashes are dismissed when the user changes pages.