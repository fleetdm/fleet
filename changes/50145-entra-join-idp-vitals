* Populated IdP host vitals (username, full name, groups, department) on Entra-joined Windows hosts enrolled without Fleet MDM, by reading the Entra join user from the device on each detail refresh and matching it to the SCIM-provisioned user. Manually set and end user authentication mappings keep precedence.
