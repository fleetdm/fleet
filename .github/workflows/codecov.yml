coverage:
  status:
    project:
      default: false
      frontend:
        informational: true
        paths:
          - "frontend/"
      app:
        paths: "!frontend/"
    patch:
      default:
        informational: true
