name: Fleet CodeQL

paths-ignore:
  - website/assets/dependencies/
  # Generated Wails bindings for tools/hangar — regenerated by
  # `wails3 generate bindings` on every build, so the unused-import warnings
  # they carry can't be fixed in-tree (any edit is clobbered on regen).
  - tools/hangar/frontend/bindings/
  - docs/Contributing/
