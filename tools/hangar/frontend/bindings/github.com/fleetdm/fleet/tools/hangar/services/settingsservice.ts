// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * SettingsService exposes settings, repo probing, ngrok parsing, and the
 * sandboxed file helpers. Mirrors the settings/* commands from settings.rs.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as settings$0 from "../internal/settings/models.js";

/**
 * DetectFleetConfig returns the relative serve-config name in the repo root
 * (fleet.yml/fleet.yaml), or "" if none.
 */
export function DetectFleetConfig(repo: string): $CancellablePromise<string> {
    return $Call.ByID(3638141122, repo);
}

/**
 * GetSettings loads the persisted settings (defaults if none saved yet).
 */
export function GetSettings(): $CancellablePromise<settings$0.Settings> {
    return $Call.ByID(3703475196).then(($result: any) => {
        return $$createType0($result);
    });
}

/**
 * NewServerProfile returns a fresh server profile for the next free slot
 * (canonical-but-offset ports, compose project, color, serve config), leaving
 * name/worktree for the caller to fill before saving. Errors if the server cap
 * is already reached. The slot is derived from the currently-saved servers so
 * the new ports/project/ID never collide.
 */
export function NewServerProfile(): $CancellablePromise<settings$0.ServerProfile> {
    return $Call.ByID(3299702313).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * OpenPath opens a dir or allowed file in the system file manager.
 */
export function OpenPath(path: string, reveal: boolean): $CancellablePromise<void> {
    return $Call.ByID(1192253532, path, reveal);
}

/**
 * OpenURL opens an http(s) URL in the default browser.
 */
export function OpenURL(url: string): $CancellablePromise<void> {
    return $Call.ByID(2148168380, url);
}

/**
 * ParseNgrokYml summarizes an ngrok.yml (empty path = ngrok's default).
 */
export function ParseNgrokYml(path: string): $CancellablePromise<settings$0.NgrokYamlInfo> {
    return $Call.ByID(488399815, path).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * ProbeFleetRepo validates a single path, or (when path is empty) discovers
 * Fleet clones under the well-known dev roots.
 */
export function ProbeFleetRepo(path: string): $CancellablePromise<settings$0.RepoProbe[]> {
    return $Call.ByID(984256957, path).then(($result: any) => {
        return $$createType4($result);
    });
}

/**
 * ReadTextFile reads a .yml/.yaml file under $HOME.
 */
export function ReadTextFile(path: string): $CancellablePromise<string> {
    return $Call.ByID(2361415370, path);
}

/**
 * SaveSettings persists the given settings.
 */
export function SaveSettings($in: settings$0.Settings): $CancellablePromise<void> {
    return $Call.ByID(677885975, $in);
}

/**
 * WriteTextFile writes a .yml/.yaml file under $HOME.
 */
export function WriteTextFile(path: string, contents: string): $CancellablePromise<void> {
    return $Call.ByID(3460460295, path, contents);
}

// Private type creation functions
const $$createType0 = settings$0.Settings.createFrom;
const $$createType1 = settings$0.ServerProfile.createFrom;
const $$createType2 = settings$0.NgrokYamlInfo.createFrom;
const $$createType3 = settings$0.RepoProbe.createFrom;
const $$createType4 = $Create.Array($$createType3);
