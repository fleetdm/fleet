apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "8080"
  labels:
    app: kolide-pr-{{ .Number }}
  name: zzzzz{{ .Number }}-kolide-kolide-net
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: kolide-pr-{{ .Number }}
  sessionAffinity: None
