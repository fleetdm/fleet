import Styles from '../../styles';

const { padding } = Styles;

export default {
  avatarStyles: {
    marginRight: padding.xSmall,
    verticalAlign: 'middle',
  },
  containerStyles: {
    paddingLeft: '24px',
  },
};
