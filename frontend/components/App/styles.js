import styles from '../../styles';

const { color } = styles;

export default {
  footerStyles: {
    alignItems: 'center',
    backgroundColor: color.darkGrey,
    height: '55px',
    textAlign: 'center',
    position: 'absolute',
    paddingTop: '15px',
    left: '0',
    right: '0',
    bottom: '0',
  },
};
