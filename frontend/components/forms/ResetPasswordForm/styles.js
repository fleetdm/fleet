import Styles from '../../../styles';

const { padding } = Styles;

export default {
  buttonStyles: {
    marginTop: padding.base,
  },
  formStyles: {
    width: '100%',
  },
  inputStyles: {
    width: '100%',
  },
};
